# 継続編集 → GitHub 反映ワークフロー

「Claude で編集し続けて、最新形を GitHub に反映する」ための運用手順です。

## 大前提（重要）

Claude の作業領域はセッションごとに初期化されます。そのため **Claude 上のファイルを“置きっぱなしで”編集し続けることはできません。** 継続的に回すには、**GitHub を唯一の正本（source of truth）** とし、

```
GitHub（正本） → Claude が読み込んで編集 → あなたがコミット → GitHub 更新 → Pages 自動再公開
```

というループにします。この zip は、その出発点となる「現時点の最新一式」です。まず GitHub に上げてベースラインを作ってください。

---

## 1. 最初の一回だけ：GitHub に土台を作る

1. GitHub で **`leokurata.github.io`** という名前のリポジトリを作成（Public）。
   - この名前にすると公開 URL が `https://leokurata.github.io/` になり、すべてのパスが素直に通ります（canonical 等の設定もこの前提）。
2. この zip の**中身**（`index.html`・`ja/`・`assets/` などすべて）をリポジトリ直下にアップロード。
3. **Settings → Pages** で Source を `Deploy from a branch` → `main` / `/(root)` に設定して保存。
4. 数分で `https://leokurata.github.io/` に公開されます。

（詳細は同梱の `README.md` を参照）

---

## 2. 毎回のループ：編集して反映する

### 手順
1. **Claude に依頼する。** 変更内容を伝えます。最新の公開版から始めたいときは、こう言ってください:
   > 「`https://github.com/leokurata/leokurata.github.io` の最新を読み込んで、○○を直して」

   Claude は**あなたの公開リポジトリを直接読めます**（`raw.githubusercontent.com` などから取得）。毎回ファイルを貼り直す必要はありません。※非公開リポジトリの場合は中身を貼り付けるか、後述の Claude Code をご利用ください。
2. **Claude が更新後のファイル**（全文、または差分・zip）を返します。
3. **あなたがコミットする。** いちばん簡単なのは GitHub のウェブ編集です:
   - 対象ファイルを開き、鉛筆アイコン（Edit）→ 内容を貼り替え → **Commit changes**。
   - 複数ファイルをまとめて編集したいときは、リポジトリ画面で **`.`（ピリオド）キー** を押すとブラウザ版 VS Code（github.dev）が開きます。編集してコミット。
   - ローカル派なら `git pull → 置き換え → git commit → git push`。
4. コミットすると **GitHub Pages が約1分で自動再公開** します。

### 大切なコツ
- **編集セッションを始める前に、手元の変更が必ず GitHub にコミット済みであること。** そうしないと Claude が古い版を編集してしまい、せっかくの修正が巻き戻ります。GitHub 上の版を常に「最新・正」に保ってください。

---

## 3. Claude はコミット（push）まで代行できる？

この通常チャットでは **できません。** push にはあなたの GitHub 認証情報が必要で、Claude はパスワードやトークンを扱いません。コミット操作はあなたが行ってください（手順は上記2の3）。

「編集からコミットまで一箇所で完結させたい」場合は、次の **Claude Code** が向いています。あなた自身の GitHub ログインで認証され、リポジトリへの編集・コミット・push までを Claude が直接行えます（ローカルに Node.js などの準備が必要）。

---

## 4. よく使う依頼の例

- 「リポジトリの最新を読んで、発表に △△ を1件追加して」
- 「`index.html` の working paper の PDF リンク（`#`）を、この URL に置き換えて」
- 「英語版の変更を日本語版にも反映して」
- 「現在のプロジェクト欄を、この文章＋この数式に差し替えて」

---

## 補足：いま残っているプレースホルダ

公開前後に埋めると良い箇所:
- 各論文の PDF/リンク（`<a href="#">` の `#`）
- `assets/cv.pdf`（CV の PDF を置く）
- About 本文（`［…］` の段落）／現在のプロジェクト欄（英語版に残るサンプル数式は社会的選択理論向けに差し替え可）
- 指導教員名の漢字表記（日本語版のコメント箇所）
- 研究室住所、必要なら顔写真（`assets/img/profile.svg` を `profile.jpg` 等に差し替え）
